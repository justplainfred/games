{
  "name": "@r-t-p/games",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "prepack": "npx lerna run test",
    "publish": "npx lerna publish"
  }
}
